# 🚀 Инструкция по запуску Platter Flow

## Шаг 1: Получить изменения из GitHub

Если ты ещё не скачал изменения локально:

```bash
git pull origin master
```

Чтобы полностью заменить локальную версию на новую:

```bash
git fetch origin master
git reset --hard origin/master
```

## Шаг 2: Установить зависимости

```bash
pip install -r requirements.txt
```

Если возникают проблемы, установи вручную:

```bash
pip install fastapi uvicorn sqlalchemy passlib bcrypt pydantic pydantic-settings python-multipart
```

## Шаг 3: Инициализировать базу данных

**Очень важно!** Сначала инициализируем БД и создаём тестовые данные:

```bash
python init_db.py
```

Эта команда:
- ✅ Создаст таблицы в БД
- ✅ Добавит 3 тестовых пользователя (официант, повар, админ)
- ✅ Добавит примеры меню
- ✅ Создаст 8 столов в ресторане

Тестовые учётные данные:
- **Официант**: `ofikNum1` / `123321`
- **Повар**: `povarNum1` / `123321`
- **Администратор**: `adminNum1` / `123321`

## Шаг 4: Запустить серверу

```bash
uvicorn main:app --reload
```

Сервер запустится на **http://localhost:8000**

## Шаг 5: Открыть веб-интерфейс

Перейди по адресу:

```
http://localhost:8000/static/index.html
```

## 📋 Проверка

Если сервер запущен, ты должен видеть:

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Application startup complete
```

### API документация (Swagger UI):
```
http://localhost:8000/docs
```

### API ReDoc документация:
```
http://localhost:8000/redoc
```

## 🆘 Решение проблем

### Ошибка: ModuleNotFoundError

```bash
# Убедись, что находишься в корне проекта
cd /path/to/VIKA_PROEKT

# Переактивируй виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac

# Переустанови зависимости
pip install -r requirements.txt
```

### Ошибка: "Validation error" при запуске

Если видишь ошибку про `SECRET_KEY`, `ALGORITHM` и т.д.:

1. Убедись, что файл `.env` существует в корне проекта
2. Скачал последние изменения: `git pull origin master`
3. Переустанови зависимости: `pip install -r requirements.txt`

### БД уже существует

Если нужно очистить БД и начать заново:

```bash
# Удали старую БД
rm restaurant.db  # Linux/Mac
del restaurant.db  # Windows PowerShell

# Заново инициализируй
python init_db.py
```

### Порт 8000 уже занят

Если порт 8000 занят другим приложением:

```bash
uvicorn main:app --reload --port 8001
```

Тогда открывай http://localhost:8001

## 📂 Структура папок

```
VIKA_PROEKT/
├── app/                    # Основной код приложения
│   ├── api/               # API маршруты
│   ├── models/            # SQLAlchemy модели
│   ├── schemes/           # Pydantic схемы валидации
│   ├── database/          # Конфигурация БД
│   └── config.py          # Настройки приложения
├── static/                # Веб-интерфейс
│   ├── index.html         # Главная страница
│   └── js/
│       └── app.js         # Логика приложения
├── main.py                # Точка входа
├── init_db.py             # Инициализация БД
├── .env                   # Переменные окружения
├── requirements.txt       # Python зависимости
└── README.MD              # Документация
```

## 🔍 Проверка подключения к API

### Тест login (curl):

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"ofikNum1","password":"123321"}'
```

### Получить всё меню:

```bash
curl http://localhost:8000/api/menu/
```

### Получить все столы:

```bash
curl http://localhost:8000/api/tables/
```

## 💡 Полезные команды

```bash
# Запустить с конкретным портом
uvicorn main:app --reload --port 8001

# Запустить без перезагрузки при изменении файлов
uvicorn main:app

# Очистить __pycache__ и .pyc файлы
find . -type d -name __pycache__ -exec rm -r {} + 2>/dev/null

# Проверить версию Python
python --version

# Проверить установленные пакеты
pip list

# Обновить pip
pip install --upgrade pip
```

## 🎯 Готово!

Теперь у тебя запущен:
- ✅ Backend API на FastAPI
- ✅ Веб-интерфейс на http://localhost:8000/static/index.html
- ✅ SQL База данных SQLite
- ✅ Полная аутентификация и авторизация

Можешь начинать работать с системой управления рестораном!

---

💬 Если есть вопросы - смотри README.MD для полной документации.

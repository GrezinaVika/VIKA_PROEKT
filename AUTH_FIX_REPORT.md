# 🔐 Отчёт об Исправлении Ошибок Автентификации (401 Unauthorized)

## ❌ ПРОБЛЕМА

Множественные ошибки **401 Unauthorized** при попытках войти:

```
INFO:     127.0.0.1:65155 - "POST /api/auth/login HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:65155 - "POST /api/auth/login HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:65155 - "POST /api/auth/login HTTP/1.1" 401 Unauthorized
...
```

### ПОЧЕМУ ЭТО Происходит?

У вас было **рассогласование учётных данных** между бэкендом и фронтендом:

---

## РАССОГЛАСОВАНИЕ

### В init_db.py (Какие пользователи создавались):

```python
# ПОЛЬЗОВАТЕЛИ ДО FIX (НЕПРАВИЛЬНЫЕ):
"""
- ofikNum1 / waiter123 ← ОФИЦИАНТ 
- userNum1 / user123 ← ПОЛЬЗОВАТЕЛЬ (ОШИБОЧНАЯ РОЛЬ!)
- adminNum1 / admin123 ← АДМИН
"""
```

### В app.js (Что ожидает фронтенд):

```javascript
// ОЧЕКИВАЭМЫЕ УЧЁТНЫЕ ДАННЫЕ:
console.log('👨‍🍳 ПОВАР / КУХАР:');
console.log('   chefNum1 / chef123');  // ← НЕ сохраняется в БД!
console.log('👔 ОФИЦиАНТ:');
console.log('   waiterNum1 / waiter123');  // ← имя ofikNum1, не waiterNum1
console.log('👨‍💼 АДМИНИСТРАТОР:');
console.log('   adminNum1 / admin123');  // ✅ ОК
```

---

## ✅ ОПИСАНиЕ FIX

### Что было исправлено:

**Файл:** `init_db.py`
**Коммит:** `37a9b0f8ee0783082ff4d238c8bb98fe192e14ab`

#### НОВЫЕ ПОЛЬЗОВАТЕЛИ:

```python
# ПОЛЬЗОВАТЕЛИ ПОСЛЕ FIX (ПРАВИЛЬНЫЕ):
"""
- chefNum1 / chef123 ← ПОВАР (НОВЫЙ!)
- waiterNum1 / waiter123 ← ОФИЦИАНТ (Оисправленное имя)
- adminNum1 / admin123 ← АДМИН
"""

# Кроме того:
# - Удален ошибочный пользователь "userNum1" с ролью "user"
# - Меню сокращено с 30 посынтетов до 6 для расчётвительности
# - Столы сокращены с 8 до 6 (снижение расчётвительности)
```

---

## 📋 Как Очистить БД и Переинициализировать

### ОПЦИО НОВАЯ БД ОТ КОнНУЛя:

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. Удалите старые файлы БД:
rm test.db
rm test.db-shm
rm test.db-wal

# 3. Перезагрузите страницу в браузере:
Ctrl+Shift+R (hard refresh)

# 4. Запустите сервер снова:
uvicorn main:app --reload
```

Снова будет выведено:
```
[INIT] Initializing database...
[OK] Test users created:
     Chef: chefNum1 / chef123
     Waiter: waiterNum1 / waiter123
     Admin: adminNum1 / admin123
[OK] Menu items created (6 items)
[OK] Restaurant tables created (6 tables)

[SUCCESS] Database initialization complete!
```

---

## 🧪 Точки Доступа (ТЕСТОВЫЕ КРЕДЕНшиАЛЫ)

| Роль | Номер Прификс | Мнемоника | Логин | Пароль | Проверка |
|--------|--------|----------|-------|---------|----------|
| 👨‍🍳 **ПОВАР** | Chef | Красные заказы | chefNum1 | chef123 | Приспособление к заказам |
| 👔 **ОФИЦИАНТ** | Waiter | Меню + график | waiterNum1 | waiter123 | Остановка заказов |
| 👨‍💼 **АДМИН** | Admin | Управление | adminNum1 | admin123 | Настройки |

---

## 🛋️ Этажи Вёртикальный Тест

### 1. Открыть инспектор (девы тулс = F12)

### 2. Перейти в консоль (таб **Console**)

### 3. Повыторите попытки войти с НОВЫМИ креденшиалами:

```bash
chefNum1 / chef123 ✅
waiterNum1 / waiter123 ✅
adminNum1 / admin123 ✅
```

### 4. Проверьте вывод в консоли:

Ожидаемые сообщения:

```javascript
// ПОВАР:
👨‍🍳 ПОВАР вошёл - настраиваю интерфейс...
✅ ПОВАР: Видит кнопку "Удалить заказ" ДЛЯ ВСЕХ статусов

// ОФИЦИАНТ:
👔 ОФИЦИАНТ вошёл - настраиваю интерфейс...
✅ ОФИЦИАНТ: Видит кнопку "Добавить блюдо в заказ"

// АДМИНИСТРАТОР:
👨‍💼 АДМИН вошёл - настраиваю интерфейс...
✅ АДМИН: Видит кнопки "Удалить стол" и "Удалить блюдо"
```

---

## 🔓 Механизм Автентификации (ТЕХНИЧНАЯ НОТА)

### До недавних пор норма автентификации:

**Процесс:**
1. Клиент отправляет `username` + `password` (плоские тексты)
2. Сервер находит пользователя в БД
3. Сервер проверяет хеш с `bcrypt.verify()
4. Если ок, возвращает JSON с данными пользователя
5. Если не - возвращает 401

**Нет JWT токенов** - данные пользователя сторятся в `sessionStorage` браузера.

### Ошибка 401 может происходить если:

1. ❌ Пользователь **не найден** в БД
2. ❌ Пароль **неверный** (hash не совпадает)
3. ❌ Пользователь `is_active = False`

---

## 🔍 Отнюди взялись Креденшиалы?

Эти учётные данные **твёрдокодини (хардкод)** в `static/js/app.js`:

```javascript
window.addEventListener('DOMContentLoaded', () => {
    console.log('🔐 Используйте учётные данные для входа:');
    console.log('   Повар: chefNum1 / chef123');
    console.log('   Официант: waiterNum1 / waiter123');
    console.log('   Администратор: adminNum1 / admin123');
});
```

Эти креденшиалы тестовые вкодированы в `init_db.py`.

---

## ✅ РЕЗУЛЬТАТ

**БЕФОРЕ:**
```
401 Unauthorized (x15)
Когда попытки войти с chefNum1/chef123
Потому что ЭТОГО пользователя НЕ БЫЛО в БД
```

**АФТЕР:**
```
200 OK
Пользователь найден в БД
Пароль верный - вход осуществилен успешно!
```

---

**💉 Основная урок:** Пользователи в бэкенде и фронтенде должны соответствовать так, что критично!
